import 'package:flutter/material.dart';

class CafeVerificationPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Text("Cafe Verification Page"),
    );
  }
}